
{% block body %}
<style>
    :root{
        --bg:#0f172a;
        --card1:#111827;
        --card2:#1f2933;
        --muted:#9ca3af;
        --text:#e5e7eb;
        --primary:#2563eb;
        --accent:#22c55e;
    }
    body{background:var(--bg);color:var(--text);font-family:'Segoe UI',sans-serif}
    .container{max-width:1200px;margin:auto;padding:40px 20px}
    .page-title{text-align:center;font-size:2.5rem;margin-bottom:40px}
    .teams-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:25px}
    .team-card{background:linear-gradient(145deg,var(--card1),var(--card2));border-radius:16px;padding:25px;box-shadow:0 10px 30px rgba(0,0,0,.4);transition:transform .3s,box-shadow .3s}
    .team-card:hover{transform:translateY(-6px);box-shadow:0 20px 40px rgba(0,0,0,.6)}
    .team-header{display:flex;justify-content:flex-end}
    .team-id{background:var(--primary);padding:5px 12px;border-radius:999px;font-size:.85rem}
    .team-name{margin:20px 0;font-size:1.5rem;text-align:center}
    .team-actions{display:flex;gap:12px;justify-content:center}
    .btn{padding:10px 18px;border-radius:10px;font-weight:600;transition:.3s;border:none;cursor:pointer}
    .btn-view{background:#374151;color:var(--text)}.btn-view:hover{background:#4b5563}
    .btn-join{background:var(--accent);color:#052e16}.btn-join:hover{background:#16a34a}
</style>

<<<<<<< HEAD
<div class="container">
    <h1 class="page-title">🏆 Teams</h1>

    <div class="teams-grid">
=======
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actionss</th>
            </tr>
        </thead>
        <tbody>
>>>>>>> 9eab78729dd86b66264bb1db6345d414f242b452
        {% for equipe in equipes %}
            {% include 'equipe/card.html.twig' with { equipe: equipe } %}
        {% endfor %}
    </div>
    {% if equipes is empty %}
        <p class="empty">No teams yet.</p>
    {% endif %}
</div>

{% include 'equipe/_modal_show.html.twig' %}

<script src="{{ asset('js/equipe.js') }}"></script>
{% endblock %}